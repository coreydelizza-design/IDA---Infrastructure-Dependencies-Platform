# Prototype defaults. The current UI runs entirely from typed fixture data.
VITE_DATA_MODE=mock
VITE_API_BASE_URL=http://localhost:4010

# Reserved for the production API phase. Never expose server secrets through VITE_* values.
PORT=4010
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ida
LOG_LEVEL=info
