// SCAFFOLD: concrete Supabase repository implementations.
//
// These implement the ports in src/application/ports.ts against the Supabase
// client using the mappers in ../mappers and the schema in supabase/migrations.
// Wired in a later step; until then src/infrastructure/supabase/client.ts
// returns a not-configured repository set and the app runs in local mode.

export {};
